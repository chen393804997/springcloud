server:
  port: 1113
spring:
  application:
    name: hello-service
eureka:
  client:
    serviceUrl:
      defaultZone: @eureka.client.serviceUrl.defaultZone@
  instance:
    preferIpAddress: true
    hostname: hello-service